<UserControl x:Class="SurveillanceIndexer.Views.Components.VideoQueueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="300">

    <Grid Background="#f0f0f0">
        <StackPanel Margin="5">
            <Label Content="Processing Queue" FontWeight="Bold"/>
            <ListBox ItemsSource="{Binding Items}" Height="300" AllowDrop="True" Drop="OnDrop">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding FileName}"/>
                            <TextBlock Grid.Column="1" Text="{Binding Status}" Foreground="Gray"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="Start Processing" Click="Start_Click"/>
        </StackPanel>
    </Grid>
</UserControl>